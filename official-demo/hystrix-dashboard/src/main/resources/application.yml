spring:
  application:
    name: HYSTRIX.DASHBOARD

server:
  port: 9001

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/

